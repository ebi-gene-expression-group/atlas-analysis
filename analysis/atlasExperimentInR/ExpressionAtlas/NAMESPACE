export( summarizeAtlasExperiment, parseAtlasXML, createAtlasSiteConfig, createArrayExpressSiteConfig )
exportClasses( Analytics, AssayGroup )
export( platform, assay_groups, getAllAssays, assay_group_id, assay_names, assay_group_label )
import( Biobase, GenomicRanges, limma, plyr, XML, yaml, methods )
